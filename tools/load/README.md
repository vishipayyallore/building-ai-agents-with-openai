# Load testing helpers

Optional local load-testing assets for production and Session 7 demo prep.

Use this folder for small reproducible load scripts or notes such as:

- `hey -n 10 -c 2 http://localhost:8000/api/chat`
- Locust files for repeatable dashboard demos

Keep the directory lightweight. The canonical teaching content for production foundations stays in `docs/12-production.md` and `docs/02-master-plan.md`.
